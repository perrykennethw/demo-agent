# Calculator App

This application is a simple command-line calculator. It takes a mathematical expression as an argument and returns the evaluated result.

## Usage

To use the calculator, run `main.py` with your expression enclosed in quotes:

```bash
python main.py "3 + 5"
```

## Example

```bash
python main.py "10 / 2 + (3 * 4)"
```
